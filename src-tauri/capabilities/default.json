{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop window capability for OneBoard Privacy Guard.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
